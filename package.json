{
  "name": "digibillmate-engine",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "DigiBillMate website builder engine — Supabase schema and build pipeline",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "postinstall": "npm --prefix site-builder install",
    "export:site": "node --env-file-if-exists=.env scripts/export-site.mjs",
    "build:site": "node --env-file-if-exists=.env scripts/build-site.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0"
  }
}
